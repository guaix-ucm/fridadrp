
version: 2
jobs:
  # using tox
  toxify:
      docker:
        - image: python:3.6
      steps:
        - checkout
        - run: pip install numina
        - run: pip install tox && tox

workflows:
  version: 2
  fridadrp:
    jobs:
      - toxify
